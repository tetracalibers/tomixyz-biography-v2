---
title: React Router v7のactionとTanstack Query
date: "2025-10-23"
description: React Routerだけでは難しいと感じたこと
tags:
  - react-router
  - tanstack-query
references:
  - title: react-router 使う機能、使わない機能
    url: https://blog.ojisan.io/react-router-using/
series: summario-dev
draft: true
---

React Router v7では、データフェッチとその状態管理をフレームワーク側が担ってくれる仕組みが搭載されています。
その仕組みにうまく乗せることで、APIフェッチを行う`useEffect`や、取得したデータやエラー状態などを保持する`useState`を使わずに済みます。

圧倒的に充実しているReactのエコシステム（UIライブラリ等）を活用しながらも、React特有のつらいポイントを避けることができるのは大きなアドバンテージだと感じ、Next.jsではなくReact Router v7を選びました。

しかし、1つのページに多数の機能を備えるアプリケーションでは、React Router v7の仕組みにうまく乗せられない部分も多く、結局データフェッチとその状態管理をTanstack Queryで補強する方針をとっています。

## React Router v7で脱`useState`/`useEffect`できるか？

React Router v7のアプローチを知るには、Address Bookという公式のチュートリアルを一読するのがおすすめです。

- [Address Book](https://reactrouter.com/tutorials/address-book)

このチュートリアルで登場する次の言葉が印象的で、React Router v7が単にReactの機能に依存するフレームワークではないことが窺えます。

> 🧐 Wait a sec ... How did the sidebar update? Where did we call the `action` function? Where's the code to re-fetch the data? Where are `useState`, `onSubmit` and `useEffect`?!

実際、このチュートリアルでは`useEffect`や`useState`はほとんど登場しません。
巷のブログでも、React Router v7の仕組みをうまく使えば、タブUIやモーダルUIでさえ`useState`を使わずに実装できると評されています。

- [React Router v7で実現するuseStateゼロ開発](https://zenn.dev/dorarep/articles/1dedd5c1b9ee82)
- [React Router 7: Nested Routes](https://www.robinwieruch.de/react-router-nested-routes/)

しかし、このような`useState`を使わない実装を叶える前提として、｢URLに状態を乗せること」が求められてきます。
1つの機能に対し1つのURLを対応づけることが理想形であり、それができない場合のベストプラクティスについてはまだ情報が少ないと感じています。

### ルートモジュールの制約

React Router v7（のFramework Mode）では、ページを表すコンポーネントとページのURLを`routes.ts`という設定ファイルで対応づけます。

この`routes.ts`に登録したページを表すコンポーネントはルートモジュールと呼ばれ、ルートモジュールに限り、`loader`（あるいは`clientLoader`）と`action`（あるいは`clientAction`）という関数を置くことができます。
ざっくり言えば`loader`はデータの取得、`action`はデータの更新を担い、これらの結果（戻り値）はルートモジュールで`Props`として受け取れるようになっています。

ルートモジュールはページに対応するため、1ページにつき`loader`や`action`は1つずつしか置けないことになります。
（厳密にいえば、Nested Routesを使って1つのページを複数のルートモジュールの入れ子として表現することで、各ルートモジュールに`loader`と`action`を置くことができるわけですが…レイアウトを共通化する目的以外でNested Routesを使うのは、個人的にはわかりづらい実装だと感じています。）

ページで必要なすべてのデータを`loader`でロードするのはまだよいとして、ページ内の複数箇所で異なるデータ更新処理が必要な場合、「どの箇所で呼び出されたか？」によって`action`内で処理を分岐しなければなりません。

### URLに状態を乗せたくない場合

- URLに状態を乗せられるなら、`loader`だけで表示切り替えが済む
- 実装例：https://reactrouter.com/tutorials/address-book#urlsearchparams-and-get-submissions

- `FolderExprorer`でも同様に`?dir={folderId}`というURLクエリパラメータで表示の切り替えを試みた
- しかし、URLクエリパラメータはキャッシュされない
- 頻繁にクリックされ、「さっきのデータに戻る」ことも多いUIのため、毎回サーバーにリクエストされるのは避けたかった

### 初期データと新たなデータを切り替えたい場合

- `loader`で初期データを取得、ボタンが押されたら`action`で新たなデータを取得し、表示を切り替える場合を考える
- このとき、初期状態では`loaderData`を、データ取得後には`actionData`を描画したい
- `const data = actionData ?? loaderData`みたいにしてもいいけど…
- `action`でエラーが起きた場合は`actionData`にエラーの詳細が入るような場合だと、分岐が面倒なことになる

## Tanstack Queryの導入

### `useQuery`の効用

- 初期データはルートモジュールの`loader`で提供し、`useQuery`の`placeholderData`にセット
- `useQuery`の戻り値`data`は、新たなデータを取得するまでは`placeholderData`になり、データ取得後は新たなデータになる
- JSX側では`data`をそのまま描画すればいい
- 正常取得時の中身とエラー発生時の中身も分けて処理できる

- すでに取得したデータはキャッシュしておける

### `useMutation`との連携

- DBにアクセスして処理を行う`action`を`fetch`で呼び出せる必要が生じた
- つまり、各アクションにURLを割り当てる必要がある
- そこで、APIエンドポイントのように`action`だけをexportするルートモジュールを定義している
