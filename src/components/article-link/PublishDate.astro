---
import { getMonthName } from "$/utils"
import { Icon } from "astro-icon/components"

interface Props {
  date: string | Date
}

const { date } = Astro.props

const dateObj = new Date(date)
const year = dateObj.getFullYear().toString()
---

<div class="PublishDate">
  <div class="background"><Icon name="blob" width={200} /></div>
  <div class="text">
    <span class="year">{year}</span>
    <span class="month">
      {getMonthName(date)}
    </span>
  </div>
</div>

<style>
  .PublishDate {
    display: grid;
    place-items: center;
    color: light-dark(var(--gray-500), var(--gray-200));
    line-height: 1;
    font-weight: 600;
    width: 100%;
    height: 100%;
  }
  :is(.background, .text) {
    grid-column: 1 / -1;
    grid-row: 1 / -1;
  }
  .text {
    display: grid;
    place-items: center;
    gap: 0.2rem;
    padding: 2rem;
  }
  .year {
    font-size: 1.5rem;
  }
  .month {
    font-size: 1.2rem;
    font-style: italic;
  }

  .background {
    width: 100%;
    height: 100%;
    z-index: -1;
  }
  [data-icon="blob"] {
    width: 100%;
    height: 100%;
  }
</style>
