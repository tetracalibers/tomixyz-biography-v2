---
import Prose from "$components/prose/Prose.astro"
import type { CollectionEntry } from "astro:content"
import { render } from "astro:content"

interface Props {
  section: CollectionEntry<"passion">
}

const { section } = Astro.props
const { Content } = await render(section)
---

<Prose>
  <h2 class="PassionSection-title">{section.data.title}</h2>
  <div class="PassionSection-content"><Content /></div>
</Prose>

<style>
  .PassionSection-title {
    text-transform: capitalize;
    text-align: center;
    word-break: auto-phrase;
  }
  .PassionSection-content {
    max-width: 44rem;
    margin-inline: auto;

    font-size: 1.125rem;
    line-height: 1.75rem;
    line-height: 2;
  }
</style>
