---
import { getCollection, type CollectionEntry } from "astro:content"
import DefaultPageLayout from "$/layouts/default.astro"
import BlogPreviewList from "$components/preview/blog/BlogPreviewList.astro"
import Paginator from "$/components/pagination/Paginator.astro"
import { pinnedFirstDateSort } from "$/lib/sort"
import type { GetStaticPaths, Page } from "astro"
import { CATEGORY_META, PAGE_SIZE } from "$/config"

const meta = {
  ogimage: "/og/blog.png"
}

export const getStaticPaths: GetStaticPaths = async ({ paginate }) => {
  const collection = (await getCollection("blog")).sort(pinnedFirstDateSort)
  return paginate(collection, { pageSize: PAGE_SIZE })
}

interface Props {
  page: Page<CollectionEntry<"blog">>
}

const { page } = Astro.props
---

<DefaultPageLayout content={{ ...CATEGORY_META.blog }} meta={{ ...meta }}>
  <BlogPreviewList entries={page.data} />
  {PAGE_SIZE < page.total && <Paginator page={page} />}
</DefaultPageLayout>
